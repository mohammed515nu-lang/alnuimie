# 🔧 دليل حل المشاكل - Troubleshooting Guide

## ❌ خطأ: "We're sorry, but we're unable to serve your request"

هذا الخطأ قد يحدث لعدة أسباب. اتبع الخطوات التالية لحل المشكلة:

---

## 🔍 الخطوة 1: التحقق من حالة الخادم (Backend Server)

### 1.1 التحقق من أن الخادم يعمل

افتح المتصفح وانتقل إلى:
```
https://construction-backend-nw0g.onrender.com/api/health
```

**النتيجة المتوقعة:**
```json
{
  "status": "healthy",
  "database": "connected",
  "timestamp": "..."
}
```

**إذا رأيت خطأ:**
- الخادم غير متاح أو متوقف
- تحقق من Render Dashboard
- تأكد من أن الخادم في حالة "Live"

### 1.2 التحقق من API الأساسي

انتقل إلى:
```
https://construction-backend-nw0g.onrender.com/
```

**النتيجة المتوقعة:**
```json
{
  "message": "Construction Management API",
  "version": "1.0.0",
  "status": "running"
}
```

---

## 🔍 الخطوة 2: التحقق من إعدادات Frontend

### 2.1 التحقق من متغير البيئة

في ملف `.env` في المجلد الرئيسي (إن وجد)، تأكد من:
```env
REACT_APP_API_URL=https://construction-backend-nw0g.onrender.com/api
```

### 2.2 التحقق من ملف `src/utils/api.js`

تأكد من أن `API_BASE_URL` يشير إلى الخادم الصحيح:
```javascript
const API_BASE_URL = process.env.REACT_APP_API_URL ||
  (isLocal ? `http://${window.location.hostname}:4000/api` : 
   'https://construction-backend-nw0g.onrender.com/api');
```

---

## 🔍 الخطوة 3: التحقق من Console في المتصفح

1. افتح Developer Tools (F12)
2. اذهب إلى تبويب Console
3. ابحث عن أخطاء مثل:
   - `Failed to fetch`
   - `Network error`
   - `CORS error`
   - `404 Not Found`

### الأخطاء الشائعة:

#### ❌ `Failed to fetch` أو `NetworkError`
**السبب:** الخادم غير متاح أو لا يمكن الوصول إليه
**الحل:**
- تحقق من اتصالك بالإنترنت
- تحقق من أن الخادم يعمل على Render
- جرب الوصول إلى رابط API مباشرة في المتصفح

#### ❌ `CORS error`
**السبب:** مشكلة في إعدادات CORS في الخادم
**الحل:**
- تحقق من ملف `server/server.js`
- تأكد من أن `corsOptions` يسمح بالوصول من النطاق الصحيح

#### ❌ `404 Not Found`
**السبب:** المسار غير صحيح
**الحل:**
- تحقق من أن المسار في `callApi` صحيح
- تأكد من أن الخادم يستمع على المسار الصحيح

---

## 🔍 الخطوة 4: التحقق من Render Dashboard

### 4.1 حالة الخادم
1. اذهب إلى [Render Dashboard](https://dashboard.render.com)
2. اختر Service الخاص بالخادم
3. تحقق من:
   - **Status:** يجب أن يكون "Live" (أخضر)
   - **Last Deploy:** يجب أن يكون ناجح
   - **URL:** يجب أن يكون موجود

### 4.2 Logs
1. اضغط على تبويب "Logs"
2. ابحث عن أخطاء مثل:
   - `MongoDB connection error`
   - `Port already in use`
   - `Environment variables missing`

---

## 🔍 الخطوة 5: التحقق من متغيرات البيئة في Render

### 5.1 متغيرات البيئة المطلوبة

في Render Dashboard → Environment:
- `MONGODB_URI` - رابط قاعدة البيانات
- `GOOGLE_CLIENT_ID` - معرف Google OAuth
- `GOOGLE_CLIENT_SECRET` - سر Google OAuth
- `FRONTEND_URL` - رابط Frontend
- `PORT` - المنفذ (اختياري، افتراضي 4000)

### 5.2 التحقق من MongoDB
- تأكد من أن `MONGODB_URI` صحيح
- تحقق من أن قاعدة البيانات متاحة
- جرب الاتصال بقاعدة البيانات يدوياً

---

## 🔍 الخطوة 6: التحقق من Frontend Deployment

### 6.1 إذا كنت تستخدم Netlify/Vercel

#### Netlify:
1. اذهب إلى Netlify Dashboard
2. تحقق من حالة Deploy
3. ابحث عن أخطاء في Build Logs

#### Vercel:
1. اذهب إلى Vercel Dashboard
2. تحقق من حالة Deployment
3. ابحث عن أخطاء في Build Logs

### 6.2 التحقق من Build
```bash
npm run build
```

إذا فشل البناء:
- تحقق من الأخطاء في Terminal
- تأكد من تثبيت جميع الحزم: `npm install`
- تحقق من أن جميع الملفات موجودة

---

## ✅ الحلول السريعة

### الحل 1: إعادة تشغيل الخادم
1. في Render Dashboard
2. اضغط على "Manual Deploy" → "Deploy latest commit"

### الحل 2: التحقق من الخادم المحلي
إذا كنت تعمل محلياً:
```bash
cd server
npm start
```

تأكد من أن الخادم يعمل على `http://localhost:4000`

### الحل 3: مسح Cache
1. في المتصفح: Ctrl+Shift+Delete
2. مسح Cache و Cookies
3. إعادة تحميل الصفحة

### الحل 4: التحقق من Network Tab
1. افتح Developer Tools (F12)
2. اذهب إلى تبويب Network
3. حاول تنفيذ إجراء في التطبيق
4. ابحث عن الطلبات الفاشلة (باللون الأحمر)
5. اضغط على الطلب الفاشل وافحص:
   - Status Code
   - Response
   - Headers

---

## 🆘 إذا استمرت المشكلة

### معلومات مفيدة للمساعدة:
1. **Screenshot** للخطأ
2. **Console Logs** من المتصفح
3. **Network Tab** للطلبات الفاشلة
4. **Render Logs** من الخادم
5. **معلومات البيئة:**
   - هل تعمل محلياً أم على Production؟
   - ما هو المتصفح المستخدم؟
   - ما هي الخطوات التي أدت للخطأ؟

---

## 📝 ملاحظات إضافية

### تحسينات تم إضافتها:
1. ✅ معالجة أفضل للأخطاء في `src/utils/api.js`
2. ✅ ErrorBoundary لمعالجة أخطاء React
3. ✅ رسائل خطأ واضحة بالعربية
4. ✅ معالجة أخطاء الشبكة والاتصال

### للتحقق من الاتصال:
افتح Console في المتصفح واكتب:
```javascript
fetch('https://construction-backend-nw0g.onrender.com/api/health')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);
```

إذا رأيت البيانات، الخادم يعمل. إذا رأيت خطأ، المشكلة في الاتصال بالخادم.

