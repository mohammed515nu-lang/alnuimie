/**
 * جزء ثابت من المسافة السفلية لمحتوى التمرير تحت شريط التبويب العائم.
 * يُضاف إليه `insets.bottom` (المنطقة الآمنة).
 * يجب أن يبقى متوافقاً مع `app/(main)/(tabs)/_layout.tsx` (هوامش + بطاقة الشريط).
 */
export const DASHBOARD_TAB_BAR_SCROLL_BASE = 78;
