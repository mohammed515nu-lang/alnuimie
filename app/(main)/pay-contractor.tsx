import { PayContractorScreen } from '../../src/screens/wallet/PayContractorScreen';

export const options = { title: 'دفع للمقاول' };

export default function PayContractor() {
  return <PayContractorScreen />;
}
