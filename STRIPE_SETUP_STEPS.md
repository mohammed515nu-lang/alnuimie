# ⚡ خطوات إصلاح Stripe - تنفيذ سريع

## ✅ الخطوات المطلوبة في Render

### 1️⃣ اذهب إلى Render Dashboard
افتح: https://dashboard.render.com

### 2️⃣ افتح Backend Service
- ابحث عن Service اسمه `construction-backend` أو مشابه
- اضغط عليه

### 3️⃣ أضف Environment Variable
1. اضغط على تبويب **"Environment"** من القائمة الجانبية
2. اضغط **"Add Environment Variable"**
3. أضف:
   - **Key**: `STRIPE_SECRET_KEY`
   - **Value**: `sk_test_...` (مفتاح Stripe الخاص بك)
4. اضغط **"Save Changes"**

### 4️⃣ احصل على Stripe Secret Key
إذا لم يكن لديك مفتاح:
1. اذهب إلى: https://dashboard.stripe.com/test/apikeys
2. انسخ **Secret key** (يبدأ بـ `sk_test_`)
3. الصقه في Render

### 5️⃣ انتظر إعادة التشغيل
- Render سيعيد تشغيل الخدمة تلقائياً
- انتظر 2-5 دقائق حتى يكتمل النشر

### 6️⃣ اختبر
افتح في المتصفح:
```
https://construction-backend-nw0g.onrender.com/api/stripe/create-payment-intent
```

**النتيجة المتوقعة:**
- ✅ إذا رأيت خطأ 400 أو 401 = الـ route يعمل! ✅
- ❌ إذا رأيت 404 = المشكلة ما زالت موجودة

---

## 🔍 التحقق من Logs

إذا استمرت المشكلة:

1. في Render Dashboard → Backend Service
2. اضغط تبويب **"Logs"**
3. ابحث عن:
   - `STRIPE_SECRET_KEY is not set` ← يعني المفتاح غير موجود
   - `Cannot find module './routes/stripe'` ← يعني الملف غير موجود
   - أي أخطاء أخرى

---

## ✅ قائمة سريعة

- [ ] فتح Render Dashboard
- [ ] فتح Backend Service
- [ ] إضافة `STRIPE_SECRET_KEY` في Environment
- [ ] حفظ التغييرات
- [ ] انتظار إعادة التشغيل (2-5 دقائق)
- [ ] اختبار الـ route

---

**⏱️ الوقت المتوقع: 5-10 دقائق**
