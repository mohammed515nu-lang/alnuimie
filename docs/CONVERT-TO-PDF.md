# 📄 كيفية تحويل الملفات إلى PDF

## الطريقة 1: استخدام Microsoft Word (الأسهل) ⭐

### الخطوات:
1. **افتح الملف:**
   - افتح `THESIS-CHAPTER-3.md` في أي محرر نصوص
   - أو افتحه في VS Code

2. **انسخ المحتوى:**
   - اضغط `Ctrl+A` لتحديد الكل
   - اضغط `Ctrl+C` للنسخ

3. **الصق في Word:**
   - افتح Microsoft Word
   - اضغط `Ctrl+V` للصق
   - قم بتنسيق النص حسب الحاجة

4. **احفظ كـ PDF:**
   - File → Save As
   - اختر "PDF" من قائمة "Save as type"
   - اختر المكان
   - اضغط "Save"

---

## الطريقة 2: استخدام VS Code Extension

### تثبيت Extension:
1. افتح VS Code
2. اذهب إلى Extensions (Ctrl+Shift+X)
3. ابحث عن "Markdown PDF"
4. اضغط "Install"

### الاستخدام:
1. افتح ملف `.md`
2. اضغط `Ctrl+Shift+P`
3. اكتب "Markdown PDF: Export (pdf)"
4. اضغط Enter
5. سيتم إنشاء ملف PDF في نفس المجلد

---

## الطريقة 3: استخدام Online Converter

### مواقع مجانية:
1. **Markdown to PDF:**
   - https://www.markdowntopdf.com
   - ارفع الملف أو الصق المحتوى
   - اضغط "Convert"
   - حمّل PDF

2. **Dillinger:**
   - https://dillinger.io
   - الصق المحتوى
   - Export → PDF

3. **StackEdit:**
   - https://stackedit.io
   - الصق المحتوى
   - Export → PDF

---

## الطريقة 4: استخدام Pandoc (للمحترفين)

### تثبيت Pandoc:
```bash
# Windows (using Chocolatey)
choco install pandoc

# أو حمّل من: https://pandoc.org/installing.html
```

### التحويل:
```bash
pandoc THESIS-CHAPTER-3.md -o THESIS-CHAPTER-3.pdf
```

---

## الطريقة 5: استخدام Browser Print

### الخطوات:
1. **افتح الملف في VS Code:**
   - اضغط `Ctrl+Shift+V` لمعاينة Markdown
   - أو استخدم Preview

2. **Print:**
   - اضغط `Ctrl+P`
   - اختر "Microsoft Print to PDF"
   - اضغط "Print"
   - احفظ الملف

---

## الطريقة 6: استخدام Python Script

### تثبيت المكتبات:
```bash
pip install markdown pdfkit
```

### السكريبت:
```python
import markdown
import pdfkit

# قراءة ملف Markdown
with open('THESIS-CHAPTER-3.md', 'r', encoding='utf-8') as f:
    md_content = f.read()

# تحويل Markdown إلى HTML
html = markdown.markdown(md_content)

# تحويل HTML إلى PDF
pdfkit.from_string(html, 'THESIS-CHAPTER-3.pdf')
```

---

## 🎯 التوصية

**للحصول على أفضل نتيجة:**
1. استخدم **Microsoft Word** (الطريقة 1)
2. قم بتنسيق النص بشكل احترافي
3. أضف الصفحات والترقيم
4. احفظ كـ PDF

---

## 📝 نصائح للتنسيق في Word

### 1. العناوين:
- الفصل: حجم 18، Bold
- القسم: حجم 16، Bold
- القسم الفرعي: حجم 14، Bold

### 2. النص:
- حجم الخط: 12
- نوع الخط: Arial أو Times New Roman
- تباعد الأسطر: 1.5

### 3. الصفحات:
- الهوامش: 2.5 سم من كل جانب
- ترقيم الصفحات: أسفل الصفحة

### 4. الجداول:
- ترقيم الجداول
- عنوان واضح
- خط واضح

---

## ✅ Checklist قبل التحويل

- [ ] تم مراجعة المحتوى
- [ ] لا توجد أخطاء إملائية
- [ ] التنسيق صحيح
- [ ] الصور موجودة (إن وجدت)
- [ ] الجداول منسقة
- [ ] الترقيم صحيح

---

**تم الإنشاء:** 2024










